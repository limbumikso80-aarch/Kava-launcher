<?xml version="1.0" encoding="utf-8"?>
<!--
    AndroidManifest.xml — KavaLauncher v2 (with multi-version launcher feature)
    Changes from original:
      • Added VersionLauncherActivity declaration
      • Added REQUEST_INSTALL_PACKAGES permission (needed for fallback APK install)
      • MANAGE_EXTERNAL_STORAGE already present — used for scanning /sdcard
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.kava.launcher">

    <!-- Existing permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.KILL_BACKGROUND_PROCESSES" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission
        android:name="android.permission.MANAGE_EXTERNAL_STORAGE"
        tools:ignore="ScopedStorage" />
    <uses-permission
        android:name="android.permission.QUERY_ALL_PACKAGES"
        tools:ignore="QueryAllPackagesPermission" />

    <!--
        REQUEST_INSTALL_PACKAGES
        Required on API 26+ for the system install-prompt fallback in
        ApkVersionManager.launchInstallFallback().  Without this permission
        the PackageInstaller Intent is silently rejected on Android 8+.
    -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:extractNativeLibs="false"
        android:hardwareAccelerated="true"
        android:largeHeap="true"
        android:requestLegacyExternalStorage="true">

        <!-- ── Existing activities ─────────────────────────────────────── -->

        <activity
            android:name=".SplashActivity"
            android:screenOrientation="landscape"
            android:stateNotNeeded="true"
            android:noHistory="true"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".OnboardingActivity"
            android:screenOrientation="landscape"
            android:noHistory="true"
            android:theme="@style/AppTheme" />

        <activity
            android:name=".MainActivity"
            android:screenOrientation="landscape"
            android:configChanges="keyboard|keyboardHidden|orientation|screenSize|screenLayout|uiMode"
            android:windowSoftInputMode="adjustResize" />

        <activity
            android:name=".SettingsActivity"
            android:screenOrientation="landscape"
            android:windowSoftInputMode="adjustResize|stateHidden"
            android:theme="@style/AppTheme" />

        <activity
            android:name=".LoadingActivity"
            android:screenOrientation="landscape"
            android:stateNotNeeded="true"
            android:noHistory="true"
            android:configChanges="keyboard|keyboardHidden|orientation|screenSize|screenLayout|uiMode"
            android:theme="@style/AppTheme" />

        <activity
            android:name=".SeedBankActivity"
            android:screenOrientation="landscape"
            android:configChanges="keyboard|keyboardHidden|orientation|screenSize|screenLayout|uiMode"
            android:windowSoftInputMode="adjustResize|stateHidden"
            android:theme="@style/AppTheme" />

        <activity
            android:name=".PrivacyPolicyActivity"
            android:screenOrientation="landscape"
            android:windowSoftInputMode="adjustResize|stateHidden"
            android:theme="@style/AppTheme" />


        <activity
            android:name=".ServerStatusActivity"
            android:screenOrientation="landscape"
            android:configChanges="keyboard|keyboardHidden|orientation|screenSize|screenLayout|uiMode"
            android:theme="@style/AppTheme" />

        <activity
            android:name=".ProfileManagerActivity"
            android:screenOrientation="landscape"
            android:configChanges="keyboard|keyboardHidden|orientation|screenSize|screenLayout|uiMode"
            android:windowSoftInputMode="adjustResize|stateHidden"
            android:theme="@style/AppTheme" />

        <!-- ── NEW: Multi-version APK launcher ───────────────────────── -->
        <!--
            VersionLauncherActivity
            Hosts the RecyclerView of imported APK files and drives
            ApkVersionManager.launch() for DexClassLoader-based loading.

            configChanges set wide so the Activity is not re-created during
            landscape-only operation (consistent with other screens).
        -->
        <activity
            android:name=".VersionLauncherActivity"
            android:screenOrientation="landscape"
            android:configChanges="keyboard|keyboardHidden|orientation|screenSize|screenLayout|uiMode"
            android:windowSoftInputMode="adjustResize|stateHidden"
            android:theme="@style/AppTheme" />

        <!-- ── Services & providers ───────────────────────────────────── -->

        <service
            android:name=".CrashGuardService"
            android:exported="false"
            android:process=":crash_guard" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="com.kava.launcher.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_provider_paths" />
        </provider>

    </application>
</manifest>
